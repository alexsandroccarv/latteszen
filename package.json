{
  "name": "latteszen",
  "private": true,
  "description": "lattesZen — organizador local de Currículo Lattes (site estático, sem build). Este package.json existe só para os testes automatizados (Playwright); o app em si continua sem dependências de runtime.",
  "scripts": {
    "test": "node tools/tests/run.mjs",
    "test:xml": "node tools/xml-export-harness.mjs && node tools/xml-export-branches.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
